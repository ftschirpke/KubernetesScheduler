package cws.k8s.scheduler.scheduler.trace;

public enum LongField {
    MEMORY,
    RESIDENT_SET_SIZE,
    VIRTUAL_MEMORY,
    PEAK_RESIDENT_SET_SIZE,
    PEAK_VIRTUAL_MEMORY,
    CHARACTERS_READ,
    CHARACTERS_WRITTEN,
    BYTES_READ,
    BYTES_WRITTEN,
    REALTIME,
}
